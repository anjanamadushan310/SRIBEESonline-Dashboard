# Same-origin base; the Vite dev server proxies /api to the FastAPI backend
# (see vite.config.ts). For production builds, set the full backend origin,
# e.g. VITE_API_URL=https://api.sribees.lk/api/v1
VITE_API_URL=/api/v1
VITE_APP_NAME=SRIBEES Admin
